# Optimization Review

![[Pasted image 20260315214359.png]]

Parameter $\mu$ is chosen so that algorithm converges to a stationary point of $J(\theta)$ which could be a local minimum $(\theta_1^0)$, global minimum $(\theta^0)$, or a saddle point $(\theta_2^0)$.
At convergence, the gradient is zero.


![[Pasted image 20260315214408.png]]
Constant cost $(J(\theta)=c)$ curves for two cases:
* In (a) the negative gradient always points to the optimum.
* In (b) it points to the optimum only at certain points resulting in corrections that follow a zigzag path.

---
**Navigation**
[[L6A-Slide-03|Previous ← Gradient Descent Algorithm]]
[[L6A-0-Table-of-Contents|↑ Lecture 6A TOC]]
[[L6A-Slide-05|Next → Quadratic Cost Functions]]